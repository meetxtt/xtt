# This is a demo binary! 

## **Do not use it for production.**

`git clone https://github.com/meetxtt/xtt.git`

`cd xtt/`

### RUN:
`chmod +x xtt_qBDHmTO`
`chmod +x install.sh`

### RUN:
`./install.sh`
(follow the setup process.)

### RUN:
`./xtt_qBDHmTO -bind 0.0.0.0 -port 4877`
(Or any other port.)
 
First time you run the `xtt_qBDHmTO` it generates main server, access-code and the user.

But you can use that generated `admin/pass` from CLI or `admin/demo123`

Attention! While it's a demo version.

**API is not protected, demo user admin/admin123 cannot be removed, access code are exposed and mutilple functions have been limited/disabled or removed in this demo version.**

**If you are interested official TG:
t.me/xttone**
